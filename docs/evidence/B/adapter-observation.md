# B07 真实教务页面只读核查记录

**状态：WAITING_HUMAN（2026-09-22）**

`nku-adapter-v1` 保持 **disabled**，生产域名白名单为空。在 B 本人完成一次有授权的真实教务页面只读核查之前，代码中不填写任何南开真实域名、路径、选择器或隐藏接口。

## 待本人核查并回填的字段

| 字段 | 当前值 | 说明 |
|---|---|---|
| 学校教务域名（精确 origin） | 未填写 | 需要本人登录后确认，不由代码猜测 |
| 课表页面 path | 未填写 | 例如确切的课表查询路由 |
| 课程区域标签/表头 | 未填写 | 必需表头、行结构 |
| 学期选择状态 | 未填写 | 当前学期如何标识 |
| 是否 iframe / 跨域 iframe | 未填写 | 跨源 iframe 无法安全读取，需降级文件导入 |
| 是否分页 | 未填写 | 分页时 coverage 只能标 partial |
| 是否只渲染当前周（虚拟列表） | 未填写 | 虚拟列表同样只能标 partial |
| 是否合并单元格 | 未填写 | 影响行解析 |
| 是否有正式导出按钮/格式 | 未填写 | 有官方导出则优先文件导入路径 |

## 核查纪律

- 只读核查，不自动登录、不抓取受保护数据、不留个人信息。
- 核查完成后才允许把 `nku-adapter-v1` 标记为 enabled 并填写精确白名单（B08）。
